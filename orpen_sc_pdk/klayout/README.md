# KLayout Technology Assets

This directory is the install target for KLayout technology assets owned by
the public PDK.

- `layers.lyp` is generated from `orpen_sc_pdk/layers.yaml`.
- `tech.lyt` registers the KLayout technology and points to `layers.lyp`.
