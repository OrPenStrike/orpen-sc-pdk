---
title: "OrPen SC PDK"
description: "Public superconducting quantum and RF PDK documentation."
---

`orpen-sc-pdk` is the public superconducting quantum/RF PDK for contributors who
need to keep a Primary Layout in a private repository while still improving
shared GDSFactory ecosystem infrastructure.

The public PDK does not migrate private layout/IP into a public repo. It owns
the public process contract: layer names, layer views, the layer stack, material
semantics, public CPW cross-sections, reusable layout helpers, public cells,
static GF+ import mechanics, and
publication-safe documentation. Private layout repositories keep real chip geometry, private
parameters, private notebooks, GDS inputs from private designs, and private
run evidence.

The intended workflow is local and reviewable: a project installs
`orpen-sc-pdk` for layout and process facts, then uses `scgsim` for semantic
geometry, mesh/config generation, solver handoff, result resolution, and
reporting. OrPen's notebooks demonstrate that boundary without owning a second
simulation runtime.

## Start By Task

- [Use the public PDK](home/pdk-responsibilities.md): process semantics, cells,
  materials, static GF+ import mechanics, and documentation.
- [Connect a private layout repository](home/ecosystem-workspace.md) without
  publishing private layout or IP.
- [Develop ecosystem features](developing-features.md) through the current
  OrPen/SCGSim responsibility boundary.
- [Read public examples](public-pdk-examples/index.md), including runnable
  Palace and AEDT notebooks backed by `scgsim`.
- [Explore layout exports](layout-viewer.qmd) in the static interactive viewer.
- [Open SCQ Design Kit](https://github.com/arfiligol/SCQ_Design), the canonical
  workspace source for cross-repository Design Targets and Knowledge.
